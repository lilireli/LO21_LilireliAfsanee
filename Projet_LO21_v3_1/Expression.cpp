#include "Constantes.h"

// unuseful


namespace Calcul{

/*void Expression::EvalExpression(){
    if(exp.at(0)!='\'' || exp.at(exp.length()-1)!='\''){
        // lever exception
        }

    QString chaine;

    for(int i=0; i<exp.length(); i++){
        if(exp.at(i)==' '){
            emit evalExp(chaine);
            chaine.clear();
        }else{
            chaine+=exp.at(i);
        }
    }
}*/

}
